# LI_LINGHAO：revision 1.1 live battery 已完成

你的 DeepSeek v2 任务已完成：45 个案例、75 次正式试验，代码检查和独立
LLM judge 的综合通过率为 **72/75（96%）**。3 条执行失败全部保留在分母中。
这表示个人运行完成，不表示团队全部 D5 对比已完成，也不表示系统没有失败。

## 配置与结果

- 日期：2026-09-17（新加坡时间），suite 创建于 2026-09-16 16:21:26 UTC。
- Agent：deepseek/deepseek-v3.2，prompt v2，live，契约 d5-live-1.1。
- 独立 judge：openai/gpt-4.1-mini，逐条评审 72 个可审阅 trial。
- 普通案例：30 次，综合通过 28/30（93.33%）。
- 负面案例：15 个各执行 3 次，共 45 次，综合通过 44/45（97.78%）。
- 执行状态：72 条 completed、2 条工具问题、1 条协议错误。
- Judge：72 通过、0 失败、0 待定；另有 3 条不可审阅，仍算综合失败。
- Agent 共 383 轮，中位数 5，最多 7；输入 959130 tokens，输出 62138 tokens。
- 未触发 8 轮上限、单 trial US$0.05 上限或成员 US$3 预算停止。
- 决策信仅写入本地，使用模拟人工确认，没有发出真实保险决定。

## 三条失败的原因

1. CLM-8952，第 3 次：重复调用 get_claim，被去重保护阻止。虽然最后正确升级人工，仍因工具违规失败。
2. CLM-16502，第 1 次：同样重复调用 get_claim。最终批准和金额正确，也不能抵消执行违规。
3. CLM-16504，第 1 次：模型自行输出禁止的 Observation 块，解析停止，未完成受控决策写入。

这些是模型遵循协议和调用工具的实际可靠性问题。没有为了提高成绩重跑失败案例，
也没有关闭保护、伪造成功或删减分母。Judge 先执行 3 次检查调用是否正常，随后
从同一缓存任务继续，其间没有重跑 agent 或替换评分。

## 文件与费用

`D5_RETURN_LI_LINGHAO.zip` 在 judge 之前封存，包含完整原始 suite。
根目录 `summary.json`、`results.csv` 保留原始 judge pending 状态；
**最终成绩请看 `judge/summary.json` 和 `judge/results.csv`**。
英文说明见 `OBSERVATIONS.md`，费用见 `cost_ledger.json`，文件哈希见
`submission_manifest.json`。之前的历史结果、预检失败和发布包全部保留。

正式 agent 标价估算 US$0.282861170，正式 judge US$0.088007600，
正式运行合计 **US$0.370868770**。加已放行的 6 次 agent 预检
US$0.026086838，合计 US$0.396955608。这些调用的 API `usage.cost`
合计为 US$0.272982860；所有调用都有该字段，但它不等于独立账单核验。

此前 revision 1.1 的三轮开发预检及最后一次预检 judge 已花费标价估算
US$0.085070142，其中已经包含上述 6 次预检。仅再加正式运行费用，
整个 revision 1.1 开发及本次正式运行为 US$0.455938912，避免重复计算。
旧契约的历史运行费用不包含在这里。

## 后续工作

你的个人 live battery 与独立评分已经完成，现在可作为团队 D5 汇总的一份结果。
还需要其他成员提交同一 revision 1.1 的运行，才能完成跨模型及 v1/v2 比较。
旧版 10/75 的结果没有被改写；新版 96% 同时涉及公共契约、解析器、判据澄清
和新的随机运行，不能作为受控实验声称模型本身提升了同样幅度。
冻结的 VALIDATION.md 记录的是发布时状态，本目录记录发布后的正式运行完成状态。
