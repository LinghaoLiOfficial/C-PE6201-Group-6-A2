Hi everyone,

Please pause formal runs using the original D5 package and use your new **revision
1.1** package instead. Our first live diagnostic exposed mismatches between the
output instructions and the grader. The shared schema and grading explanations
have now been corrected; your model assignment remains unchanged.

1. Extract your new ZIP into a fresh folder. Do not reuse an old release.json.
2. Follow the included verify/offline commands, then run the six-case preflight.
3. Submit the complete preflight folder, assignment.json and package_manifest.json
   directly to the shared GitHub repository through your own branch
   `d5/YOUR_NAME-r1.1`, and open a Draft PR targeting main.
4. Put files under `Part3_LI_LINGHAO/d5_live_submissions/YOUR_NAME/r1.1/`.
   Send me the PR link and wait for the new release.json before the 75-trial run.
5. After the full run, add your return ZIP, release.json and OBSERVATIONS.md to
   the same branch/PR. I will coordinate the independent LLM judgement.

Repository: https://github.com/LinghaoLiOfficial/C-PE6201-Group-6-A2

Keep all previous attempts and failures. Do not overwrite old evidence, change
your own prompt/model, rerun to select a better score, push directly to main, or
commit your API key/.env. If you have already spent credit on the old version,
tell me the amount before starting; no need to send your key.

Both the DeepSeek v1 and v2 jobs must use revision 1.1 for a fair comparison.
Preflight results are diagnostics, not the final measured battery.
